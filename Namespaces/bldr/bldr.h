#include <iostream>
#include <string>
#include <algorithm>

namespace bldr {
	/*	bldr = builder
	 *	What are we building?
	 *	We are building nodes that have all data types existing with one set of data, or more sets of data
	 */

	//	std::copy wrapper to all of my data structures
	//	bldr::copy(Node_0, Node_1);
	void copy() {
		std::iterator the_right_thing;
		if (the_right_thing) {}
		else {}
		std::copy(one.begin(), one.end(), two.thing());
	}
